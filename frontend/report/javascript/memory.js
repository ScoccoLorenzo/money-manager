export const months = [];

function initializeMonths(arr, startYear, endYear) {
  let months = [];
  for (let year = startYear; year <= endYear; year++) {
    //serve per rendere gli anni automatici, magari esiste qualcosa di già pronto in qualche pacchetto..
  }
}